const mongoose=require('mongoose')

const User=new mongoose.Schema({
    user_id: String,
    password: String,
    name: String,
    email: String,
    public_address: String,
    restricted: Boolean,
})

export default User;